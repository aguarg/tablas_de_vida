HACER:
- TODO FUNCIONA BIEN. Cualquier cosa que agregue, es eso lo que creó el error.

NOTA: podría hacer que cada función que hace su parte del cálculo itere por el arreglo 
recién calculado, haga un .toFixed(3) y lo meta donde deba en la tabla, pero sin tocar los arreglos que
se usan en los cálculos.
Ya empecé una función al final.

O, por ahí hacer una función aparte al final, para no complicar mas las otras funciones.
Una sola que meta todos los datos, aunque sea engorrosa.


- Hacer una caja con los resultados pero explicados. Ej "el X% de los individuos van a vivir Y".

- Agregar un botón para descargar los datos.

- darle estilo. Pero esto al final, cuando todo lo demás funcione bien.

- ver chart.js para agregar una tabla a la derecha, con los datos graficados

- hacer algo como que: si en el primer inbox se escribe "test", se usa el arreglo de prueba para los cálculos, como una forma de ver que hace la app sin saber del tema.

