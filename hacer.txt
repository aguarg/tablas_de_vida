HACER:
ERROR: no carga los datos ex en la tabla, pero si hago un console.log(), se ve que crea un arreglo con los datos. 
Varios arreglos tienen valores NaN y tengo que andar haciendo malavares con los i+2, etc para evitar los NaN y eso puede ser motivo de error.

Posibles soluciones:
1- cuando tomo los datos de nx y los meto en un arreglo, pasar el arreglo a int con Number() para asegurarme de que TODO es tratado como ints.

2- Los NaNs se van a generar por cuestiones de los cálculos. Hacer los cálculos normalmente y luego pasarle el "filtro" que remueve los NaN a TODOS los arreglos que se crean, así no jode al arreglo que sigue.

Esto significa que voy a tener que rehacer los iteradores de las funciones que calculan las cosas.


Cálculos que generan NaN: desde dx en adelante. No son errores de código, son errores de que se quiere calcular con números que no existen, como los de dx = nx - n(x + 1) que va a usar un número que no existe. 
Entonces, primero calcular, luego meter en el arreglo, y luego filtrar los NaN para dejarlo limpito para el cálculo siguiente.



- Hacer una caja con los resultados pero explicados. Ej "el X% de los individuos van a vivir Y".

- Agregar un botón para descargar los datos.

- darle estilo. Pero esto al final, cuando todo lo demás funcione bien.

- ver chart.js para agregar una tabla a la derecha, con los datos graficados

- hacer algo como que: si en el primer inbox se escribe "test", se usa el arreglo de prueba para los cálculos, como una forma de ver que hace la app sin saber del tema.

